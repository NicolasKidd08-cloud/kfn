-- ╔═══════════════════════════════════════════════════════════════════════════════╗
-- ║  NEXA - STEAL A BRAINROT EDITION                                               ║
-- ║  Game-Specific Anti-Cheat Bypass                                               ║
-- ║  Requirements: Level 7+ Executor (Delta, Synapse, etc.)                        ║
-- ╚═══════════════════════════════════════════════════════════════════════════════╝

--[[
    HOW THIS WORKS:
    Steal a Brainrot uses ByteBlox-style anti-cheat that monitors via "observeTag"
    We hook this function to return fake disconnectors, disabling the AC
    
    REQUIRED EXECUTOR FUNCTIONS:
    - getgc (garbage collector access)
    - hookfunction 
    - newcclosure
    - rawget
    
    If your executor doesn't have these, this WON'T work. Use Delta or better.
]]

-- ═══════════════════════════════════════════════════════════════════════════════
-- ANTI-CHEAT BYPASS (RUN THIS FIRST!)
-- ═══════════════════════════════════════════════════════════════════════════════

local function bypassAnticheat()
    local success, err = pcall(function()
        -- Check if executor supports required functions
        if not getgc then
            warn("❌ Your executor doesn't support getgc - need Level 7+ executor!")
            return false
        end
        if not hookfunction then
            warn("❌ Your executor doesn't support hookfunction - need Level 7+ executor!")
            return false
        end
        if not newcclosure then
            warn("❌ Your executor doesn't support newcclosure - need Level 7+ executor!")
            return false
        end
        
        local hooked = false
        
        -- Loop through garbage collected objects to find observeTag
        for _, v in pairs(getgc(true)) do
            if typeof(v) == "table" then
                local fn = rawget(v, "observeTag")
                if typeof(fn) == "function" and not hooked then
                    hooked = true
                    -- Hook the function to return fake disconnector
                    hookfunction(fn, newcclosure(function(_, _)
                        return {
                            Disconnect = function() end,
                            disconnect = function() end
                        }
                    end))
                    print("✅ ObserveTag hooked successfully!")
                end
            end
        end
        
        if not hooked then
            warn("⚠️ ObserveTag not found - AC may not be active yet or already bypassed")
        end
        
        return hooked
    end)
    
    if not success then
        warn("❌ Anticheat bypass failed:", err)
        return false
    end
    
    return true
end

-- Also try to hook remote events that trigger kicks
local function hookRemotes()
    pcall(function()
        if not hookfunction then return end
        
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        
        -- Find and hook remotes that send kick signals
        for _, remote in pairs(ReplicatedStorage:GetDescendants()) do
            if remote:IsA("RemoteEvent") then
                pcall(function()
                    local oldFire
                    oldFire = hookfunction(remote.FireServer, newcclosure(function(self, ...)
                        local args = {...}
                        -- Block X-15 and X-16 kick signals
                        if args[1] then
                            local arg = tostring(args[1]):lower()
                            if arg == "x-15" or arg == "x-16" then
                                print("🛡️ Blocked kick signal:", args[1])
                                return task.wait(9e9) -- Infinite wait = never sends
                            end
                        end
                        return oldFire(self, ...)
                    end))
                end)
            end
        end
        print("✅ Remote hooks applied!")
    end)
end

-- ═══════════════════════════════════════════════════════════════════════════════
-- SERVICES
-- ═══════════════════════════════════════════════════════════════════════════════

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")

local player = Players.LocalPlayer
local connections = {}

-- State
local savedPosition = nil
local isNoclipOn = false
local isFlyOn = false
local flySpeed = 50
local flyBV, flyBG

-- ═══════════════════════════════════════════════════════════════════════════════
-- UTILITY
-- ═══════════════════════════════════════════════════════════════════════════════

local function getChar()
    return player.Character
end

local function getHRP()
    local char = getChar()
    return char and char:FindFirstChild("HumanoidRootPart")
end

local function getHumanoid()
    local char = getChar()
    return char and char:FindFirstChildOfClass("Humanoid")
end

-- ═══════════════════════════════════════════════════════════════════════════════
-- NOCLIP (Using Humanoid State - Works after AC bypass)
-- ═══════════════════════════════════════════════════════════════════════════════

local function toggleNoclip(enabled, statusLabel)
    if connections.noclip then
        connections.noclip:Disconnect()
        connections.noclip = nil
    end
    
    if enabled then
        connections.noclip = RunService.Stepped:Connect(function()
            pcall(function()
                local hum = getHumanoid()
                if hum then
                    hum:ChangeState(11) -- StrafingNoPhysics
                end
            end)
        end)
        if statusLabel then
            statusLabel.Text = "👻 Noclip ON"
            statusLabel.TextColor3 = Color3.fromRGB(0, 255, 136)
        end
    else
        pcall(function()
            local hum = getHumanoid()
            if hum then hum:ChangeState(Enum.HumanoidStateType.Running) end
        end)
        if statusLabel then
            statusLabel.Text = "🟢 Ready"
            statusLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
        end
    end
end

-- ═══════════════════════════════════════════════════════════════════════════════
-- FLY (Works after AC bypass)
-- ═══════════════════════════════════════════════════════════════════════════════

local function toggleFly(enabled, statusLabel)
    local hrp = getHRP()
    local hum = getHumanoid()
    
    if connections.fly then
        connections.fly:Disconnect()
        connections.fly = nil
    end
    if flyBV then flyBV:Destroy() flyBV = nil end
    if flyBG then flyBG:Destroy() flyBG = nil end
    
    if enabled and hrp and hum then
        hum.PlatformStand = true
        
        flyBG = Instance.new("BodyGyro")
        flyBG.P = 9e4
        flyBG.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
        flyBG.CFrame = hrp.CFrame
        flyBG.Parent = hrp
        
        flyBV = Instance.new("BodyVelocity")
        flyBV.Velocity = Vector3.new(0, 0, 0)
        flyBV.MaxForce = Vector3.new(9e9, 9e9, 9e9)
        flyBV.Parent = hrp
        
        connections.fly = RunService.RenderStepped:Connect(function()
            if not flyBV or not flyBG then return end
            local cam = workspace.CurrentCamera
            local dir = Vector3.new(0, 0, 0)
            
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir = dir + cam.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir = dir - cam.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir = dir - cam.CFrame.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir = dir + cam.CFrame.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.Space) then dir = dir + Vector3.new(0, 1, 0) end
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then dir = dir - Vector3.new(0, 1, 0) end
            
            flyBV.Velocity = dir.Magnitude > 0 and dir.Unit * flySpeed or Vector3.new(0, 0, 0)
            flyBG.CFrame = cam.CFrame
        end)
        
        if statusLabel then
            statusLabel.Text = "🚀 Flying"
            statusLabel.TextColor3 = Color3.fromRGB(100, 200, 255)
        end
    else
        if hum then hum.PlatformStand = false end
        if statusLabel then
            statusLabel.Text = "🟢 Ready"
            statusLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
        end
    end
end

-- ═══════════════════════════════════════════════════════════════════════════════
-- TELEPORT METHODS (For Steal a Brainrot)
-- ═══════════════════════════════════════════════════════════════════════════════

-- TP to roof (gets you out of bases)
local function tpToRoof(statusLabel)
    local hrp = getHRP()
    if not hrp then return end
    
    local pos = hrp.Position
    hrp.CFrame = CFrame.new(pos.X, pos.Y + 30, pos.Z) -- Go up 30 studs
    
    if statusLabel then
        statusLabel.Text = "⬆️ Teleported to roof!"
        statusLabel.TextColor3 = Color3.fromRGB(255, 200, 100)
    end
end

-- TP into base (noclip forward)
local function tpIntoBase(statusLabel)
    local hrp = getHRP()
    if not hrp then return end
    
    -- Move forward through wall
    local lookVector = hrp.CFrame.LookVector
    hrp.CFrame = hrp.CFrame + (lookVector * 15)
    
    if statusLabel then
        statusLabel.Text = "➡️ Moved into base!"
        statusLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
    end
end

-- TP to saved position
local function tpToSaved(statusLabel)
    if not savedPosition then
        if statusLabel then
            statusLabel.Text = "❌ No position saved!"
            statusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
        end
        return
    end
    
    local hrp = getHRP()
    if not hrp then return end
    
    hrp.CFrame = CFrame.new(savedPosition)
    
    if statusLabel then
        statusLabel.Text = "✅ Teleported to base!"
        statusLabel.TextColor3 = Color3.fromRGB(0, 255, 136)
    end
end

-- Smooth TP (tween)
local function smoothTP(targetPos, statusLabel, duration)
    local hrp = getHRP()
    if not hrp then return end
    
    duration = duration or 3
    
    if statusLabel then
        statusLabel.Text = "🌀 Tweening..."
        statusLabel.TextColor3 = Color3.fromRGB(200, 150, 255)
    end
    
    local tween = TweenService:Create(hrp, TweenInfo.new(duration, Enum.EasingStyle.Linear), {CFrame = CFrame.new(targetPos)})
    tween:Play()
    tween.Completed:Connect(function()
        if statusLabel then
            statusLabel.Text = "✅ Arrived!"
            statusLabel.TextColor3 = Color3.fromRGB(0, 255, 136)
        end
    end)
end

-- ═══════════════════════════════════════════════════════════════════════════════
-- ANTI-RAGDOLL & ANTI-TRAP
-- ═══════════════════════════════════════════════════════════════════════════════

local function enableAntiRagdoll()
    pcall(function()
        local char = getChar()
        if char then
            for _, v in pairs(char:GetDescendants()) do
                if v:IsA("BallSocketConstraint") or v:IsA("HingeConstraint") then
                    v.Enabled = false
                end
            end
        end
    end)
    
    -- Also prevent future ragdolls
    if connections.antiRagdoll then return end
    connections.antiRagdoll = RunService.Heartbeat:Connect(function()
        pcall(function()
            local hum = getHumanoid()
            if hum then
                hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
                hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
            end
        end)
    end)
    print("✅ Anti-Ragdoll enabled!")
end

-- ═══════════════════════════════════════════════════════════════════════════════
-- GUI
-- ═══════════════════════════════════════════════════════════════════════════════

local function createGUI()
    local playerGui = player:WaitForChild("PlayerGui")
    
    if playerGui:FindFirstChild("NexaSAB") then
        playerGui:FindFirstChild("NexaSAB"):Destroy()
    end
    
    local gui = Instance.new("ScreenGui")
    gui.Name = "NexaSAB"
    gui.ResetOnSpawn = false
    gui.Parent = playerGui
    
    local main = Instance.new("Frame")
    main.Size = UDim2.new(0, 280, 0, 450)
    main.Position = UDim2.new(0.5, -140, 0.5, -225)
    main.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    main.BorderSizePixel = 0
    main.Parent = gui
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 12)
    corner.Parent = main
    
    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(255, 100, 100)
    stroke.Thickness = 2
    stroke.Parent = main
    
    -- Title
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0, 45)
    title.BackgroundColor3 = Color3.fromRGB(255, 80, 80)
    title.Text = "🧠 NEXA - STEAL A BRAINROT"
    title.Font = Enum.Font.GothamBlack
    title.TextSize = 14
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.Parent = main
    
    local titleCorner = Instance.new("UICorner")
    titleCorner.CornerRadius = UDim.new(0, 12)
    titleCorner.Parent = title
    
    local titleFix = Instance.new("Frame")
    titleFix.Size = UDim2.new(1, 0, 0, 15)
    titleFix.Position = UDim2.new(0, 0, 1, -15)
    titleFix.BackgroundColor3 = Color3.fromRGB(255, 80, 80)
    titleFix.BorderSizePixel = 0
    titleFix.Parent = title
    
    -- Status
    local status = Instance.new("TextLabel")
    status.Name = "Status"
    status.Size = UDim2.new(1, -20, 0, 25)
    status.Position = UDim2.new(0, 10, 0, 50)
    status.BackgroundTransparency = 1
    status.Text = "⏳ Bypassing Anti-Cheat..."
    status.Font = Enum.Font.GothamBold
    status.TextSize = 11
    status.TextColor3 = Color3.fromRGB(255, 200, 100)
    status.TextXAlignment = Enum.TextXAlignment.Left
    status.Parent = main
    
    local function createBtn(text, yPos, color)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0, 255, 0, 32)
        btn.Position = UDim2.new(0, 12, 0, yPos)
        btn.BackgroundColor3 = color or Color3.fromRGB(45, 45, 55)
        btn.Text = text
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 12
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn.Parent = main
        
        local c = Instance.new("UICorner")
        c.CornerRadius = UDim.new(0, 8)
        c.Parent = btn
        
        return btn
    end
    
    local function createLabel(text, yPos)
        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(1, -20, 0, 18)
        lbl.Position = UDim2.new(0, 10, 0, yPos)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.Font = Enum.Font.GothamBold
        lbl.TextSize = 10
        lbl.TextColor3 = Color3.fromRGB(120, 120, 130)
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.Parent = main
    end
    
    createLabel("━━━ ANTI-CHEAT ━━━", 80)
    local bypassBtn = createBtn("🛡️ Re-Apply AC Bypass", 98, Color3.fromRGB(100, 50, 50))
    
    createLabel("━━━ MOVEMENT ━━━", 138)
    local noclipBtn = createBtn("👻 Noclip: OFF", 156)
    local flyBtn = createBtn("🚀 Fly: OFF", 193)
    
    createLabel("━━━ YOUR BASE ━━━", 233)
    local saveBtn = createBtn("💾 Save Base Position", 251, Color3.fromRGB(255, 140, 0))
    local tpBaseBtn = createBtn("🏠 TP to Your Base", 288, Color3.fromRGB(0, 150, 100))
    
    createLabel("━━━ STEALING ━━━", 328)
    local noclipInBtn = createBtn("➡️ Noclip Into Base (Forward)", 346, Color3.fromRGB(100, 100, 200))
    local tpRoofBtn = createBtn("⬆️ TP to Roof (Exit Base)", 383, Color3.fromRGB(100, 150, 200))
    
    -- Info
    local info = Instance.new("TextLabel")
    info.Size = UDim2.new(1, -20, 0, 30)
    info.Position = UDim2.new(0, 10, 0, 418)
    info.BackgroundTransparency = 1
    info.Text = "Q/E = Fly Speed | Drag title to move"
    info.Font = Enum.Font.Gotham
    info.TextSize = 9
    info.TextColor3 = Color3.fromRGB(80, 80, 90)
    info.Parent = main
    
    -- Draggable
    local dragging, dragInput, mousePos, framePos
    title.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            mousePos = input.Position
            framePos = main.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    title.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement then dragInput = input end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - mousePos
            main.Position = UDim2.new(framePos.X.Scale, framePos.X.Offset + delta.X, framePos.Y.Scale, framePos.Y.Offset + delta.Y)
        end
    end)
    
    return {
        status = status,
        bypassBtn = bypassBtn,
        noclipBtn = noclipBtn,
        flyBtn = flyBtn,
        saveBtn = saveBtn,
        tpBaseBtn = tpBaseBtn,
        noclipInBtn = noclipInBtn,
        tpRoofBtn = tpRoofBtn
    }
end

-- ═══════════════════════════════════════════════════════════════════════════════
-- MAIN
-- ═══════════════════════════════════════════════════════════════════════════════

print("═══════════════════════════════════════════════════════")
print("🧠 NEXA - STEAL A BRAINROT EDITION")
print("═══════════════════════════════════════════════════════")

-- Create GUI first
local ui = createGUI()

-- Apply anti-cheat bypass
print("⏳ Applying anti-cheat bypass...")
local bypassSuccess = bypassAnticheat()
hookRemotes()
enableAntiRagdoll()

if bypassSuccess then
    ui.status.Text = "✅ Anti-Cheat Bypassed!"
    ui.status.TextColor3 = Color3.fromRGB(0, 255, 136)
else
    ui.status.Text = "⚠️ Bypass may have failed - try anyway"
    ui.status.TextColor3 = Color3.fromRGB(255, 200, 100)
end

-- Speed controls
UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.KeyCode == Enum.KeyCode.Q then
        flySpeed = math.max(10, flySpeed - 10)
        print("Fly speed:", flySpeed)
    elseif input.KeyCode == Enum.KeyCode.E then
        flySpeed = math.min(200, flySpeed + 10)
        print("Fly speed:", flySpeed)
    end
end)

-- Button connections
ui.bypassBtn.MouseButton1Click:Connect(function()
    ui.status.Text = "⏳ Re-applying bypass..."
    ui.status.TextColor3 = Color3.fromRGB(255, 200, 100)
    wait(0.5)
    bypassAnticheat()
    hookRemotes()
    ui.status.Text = "✅ Bypass re-applied!"
    ui.status.TextColor3 = Color3.fromRGB(0, 255, 136)
end)

ui.noclipBtn.MouseButton1Click:Connect(function()
    isNoclipOn = not isNoclipOn
    ui.noclipBtn.Text = isNoclipOn and "👻 Noclip: ON" or "👻 Noclip: OFF"
    ui.noclipBtn.BackgroundColor3 = isNoclipOn and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(45, 45, 55)
    toggleNoclip(isNoclipOn, ui.status)
end)

ui.flyBtn.MouseButton1Click:Connect(function()
    isFlyOn = not isFlyOn
    ui.flyBtn.Text = isFlyOn and "🚀 Fly: ON" or "🚀 Fly: OFF"
    ui.flyBtn.BackgroundColor3 = isFlyOn and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(45, 45, 55)
    toggleFly(isFlyOn, ui.status)
end)

ui.saveBtn.MouseButton1Click:Connect(function()
    local hrp = getHRP()
    if hrp then
        savedPosition = hrp.Position
        ui.status.Text = "💾 Base position saved!"
        ui.status.TextColor3 = Color3.fromRGB(0, 255, 136)
        ui.saveBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 136)
        wait(0.3)
        ui.saveBtn.BackgroundColor3 = Color3.fromRGB(255, 140, 0)
    end
end)

ui.tpBaseBtn.MouseButton1Click:Connect(function()
    tpToSaved(ui.status)
end)

ui.noclipInBtn.MouseButton1Click:Connect(function()
    -- Enable noclip briefly, move forward, disable
    toggleNoclip(true, nil)
    tpIntoBase(ui.status)
    wait(0.5)
    if not isNoclipOn then
        toggleNoclip(false, nil)
    end
end)

ui.tpRoofBtn.MouseButton1Click:Connect(function()
    tpToRoof(ui.status)
end)

-- Respawn handling
player.CharacterAdded:Connect(function()
    wait(1)
    enableAntiRagdoll()
    if isNoclipOn then
        toggleNoclip(false, nil)
        wait(0.1)
        toggleNoclip(true, ui.status)
    end
    if isFlyOn then
        toggleFly(false, nil)
        wait(0.1)
        toggleFly(true, ui.status)
    end
end)

print("═══════════════════════════════════════════════════════")
print("✅ LOADED!")
print("")
print("📋 HOW TO STEAL:")
print("  1. Save your base position first")
print("  2. Go to victim's base wall")
print("  3. Click 'Noclip Into Base'")
print("  4. Grab their brainrot")
print("  5. Click 'TP to Roof' to exit")
print("  6. Click 'TP to Your Base'")
print("")
print("⚠️ If you get kicked, try a different server!")
print("   Some servers have stricter AC")
print("═══════════════════════════════════════════════════════")


